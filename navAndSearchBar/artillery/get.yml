config:
  target: 'http://localhost:2001'
  phases:
    - duration: 60
      arrivalRate: 5
  processor: './query.js'
scenarios:
  - flow:
    - loop: 
      - function: "setupSomeData"
      - get:
          url: "/api/name?query={{ query }}"
      count: 100
